server:
  port: 8022
spring:
  application:
    name: BANKAPI
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bank
    username: root
    password: Hiep31082003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: update
  config:
    import: optional:file:.env[.properties]
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    port: 587
    username: nguyenhoanghiep478@gmail.com
    password: gqifrbhuteavupgh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


logging:
  level:
    org:
      springframework:
        security: DEBUG
        security.web:
          FilterChainProxy: DEBUG
          access:
            ExceptionTranslationFilter: DEBUG
            intercept:
              FilterSecurityInterceptor: DEBUG
          authentication:
            UsernamePasswordAuthenticationFilter: DEBUG
          firewall: DEBUG
        boot:
          autoconfigure:
            jdbc:
              DataSourceConfiguration: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
openapi:
    title: Bank API Document
    version: 2.0
    description : Bank API Service
    serverURL :
    serverName : serverTest
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
